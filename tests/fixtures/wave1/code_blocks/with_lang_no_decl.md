# Usage Guide

## Example

```python
print(1)
result = 2 + 2
print(result)
```

This block has a language tag (``python``) but no named declaration
(no ``class``, ``def``, ``async def``, etc.).  It should be **skipped** as a
``code_in_doc`` anchor and remain part of the parent section's content.
