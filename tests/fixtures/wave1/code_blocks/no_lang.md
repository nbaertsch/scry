# Configuration

## Example Config

```
key = value
another_key = another_value
third_key = third_value
```

No language tag — the block should be **skipped** as a ``code_in_doc`` anchor
and remain part of the parent section's content.
