---
scry:
  id: AUTH-LOGIN
---

# Auth Login (File B)

Second file claiming the same frontmatter id ``AUTH-LOGIN``.
Index-time validation should surface a duplicate-ID error when both files
are indexed together.
