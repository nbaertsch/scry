# Normal Heading

This is content under a real, non-empty heading.

## 

Empty heading above — the ATX marker is followed by only whitespace.
The slug derivation falls back to ``section-<short-content-hash>``.

## ?!

Punctuation-only heading above — slugification produces an empty string,
so the same hash fallback applies.
