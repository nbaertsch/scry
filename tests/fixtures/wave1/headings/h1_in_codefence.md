# Real Heading

This is real content under the real H1 anchor.

Here is a code block that contains a markdown heading — the heading inside the
fence must be **ignored** by the extractor:

```markdown
# Heading Inside Code Block

This heading is inside a fenced code block and should not become an anchor.
```

This paragraph is outside the fence and part of the "Real Heading" section.
