This file contains only paragraph text with no headings.

When no headings are present the whole file becomes one anchor whose primary ID
is the file path with no ``::heading`` suffix.  Sub-chunking applies normally
if the content exceeds ``sections.max_tokens``.

Additional paragraph to ensure there is body content.
