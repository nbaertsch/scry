# Policy Engine
<!-- scry-id: policy-engine-pinned -->

This heading has a pinned slug via an HTML comment immediately after it.
The anchor ID uses ``policy-engine-pinned`` instead of the path-derived slug.

## Rule Structure

Normal section — no ``scry-id`` override, uses path-derived slug.
